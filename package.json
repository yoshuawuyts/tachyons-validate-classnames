{
  "name": "tachyons-validate-classnames",
  "version": "1.1.2",
  "description": "Validate if a string of CSS to see if it contains valid Tachyons classnames",
  "main": "index.js",
  "scripts": {
    "test": "standard && NODE_ENV=test node test",
    "test:cov": "standard && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/tachyons-validate-class",
  "keywords": [
    "tachyons",
    "validate",
    "lint"
  ],
  "license": "MIT",
  "dependencies": {
    "cliclopts": "^1.1.1",
    "concat-stream": "^1.5.1",
    "css": "^2.2.1",
    "minimist": "^1.2.0",
    "resolve": "^1.1.7",
    "tachyons": "^4.3.1"
  },
  "devDependencies": {
    "dependency-check": "^2.6.0",
    "istanbul": "^0.4.5",
    "standard": "^9.0.2",
    "tape": "^4.6.0"
  },
  "bin": {
    "tachyons-validate-classnames": "./bin/cli.js"
  }
}
